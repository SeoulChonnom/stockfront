# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

두 부류가 모두 정식 대상이다. 인증은 외부 서비스가 발급한 JWT를 사용하고, 역할은 `user`/`admin` 두 가지다.

- **일반 사용자 — 매일 시장을 확인하는 개인 투자자.** 평일 하루 1회 이상, 미국/한국 시장의 하루 결과를 한 화면에서 빠르게 훑는다. 목표는 10~20초 안에 두 시장의 분위기를 파악하는 것. 파생 요구로 "그날 왜 그렇게 움직였는지"를 과거 날짜에서 다시 확인하는 회고 사용(주 3~5회)이 있으며, 같은 사람이 두 상황을 오간다.
- **운영자(`admin`) — 배치 품질을 확인하는 사람.** 배치 실행 후 수시로 성공/부분 실패/실패와 그 사유를 확인하고 재실행 여부를 판단한다. 운영 화면은 `ops.view` 권한이 있을 때만 내비게이션에 노출된다.

권한은 화면 노출뿐 아니라 문구까지 가른다(`src/lib/audience-copy.ts`): 같은 결손 상태라도 운영자에게는 파이프라인 용어와 원본 에러 코드를 그대로 보여주고, 일반 사용자에게는 내부 용어·복구 수단·영문 에러 코드를 노출하지 않는다. 이 분기는 제품 규칙이며 UI 구현 디테일이 아니다.

## Product Purpose

미국·한국 시장의 하루 결과를 하나의 일간 브리프 페이지로 통합해 보여준다. 지수, 핵심 이슈, AI 요약 해설, 원문 링크가 흩어져 있지 않고 한 맥락으로 연결되어야 한다.

성공은 두 가지로 정의된다. 사용자 쪽에서는 최신 브리프 한 화면에서 두 시장의 핵심을 10~20초 안에 파악하고, 아카이브 검색에서 날짜를 찾아 같은 레이아웃의 과거 브리프로 즉시 재진입할 수 있는 것. 운영 쪽에서는 배치 실패와 부분 실패 사유를 한 화면에서 판단할 수 있는 것.

## Positioning

같은 이슈를 반복 보도하는 개별 기사 목록이 아니라, **중복 제거·클러스터링을 거친 이슈 단위 카드**가 소비 단위다. 그리고 매일의 결과는 실시간 피드가 아니라 **`businessDate` 기준 스냅샷**으로 확정 저장되므로, 과거 날짜를 열어도 오늘과 완전히 같은 레이아웃·같은 정보 밀도로 재현된다. "오늘 보기"와 "그날 복기하기"가 동일한 화면 문법을 쓴다는 점이 이 제품의 구조적 차별점이다.

## Operating Context

- 화면 5종과 라우트가 제품의 기본 단위다.
  - 최신 시장 `/market/latest` — `/` 진입 시 인증 해소 후 여기로 정규화
  - 날짜별 시장 `/market/archive/:businessDate`
  - 아카이브 검색 `/market/archive/search`
  - 뉴스 클러스터 상세 `/market/cluster/:clusterId`
  - 배치 운영 `/ops/batches`
- 모든 화면은 공통 앱 셸(좌측 내비게이션 레일 / 모바일 헤더·드로어, 테마 토글) 안에서 동작한다. 운영 화면도 예외가 아니다.
- 통합 브리프 한 페이지의 정보 순서: 페이지 헤더(기준일·생성시각·상태) → 글로벌 헤드라인 → 미국 시장 섹션 → 한국 시장 섹션 → 통합 원문 기사 링크 → 메타 정보. 각 시장 섹션 내부는 섹션 헤더 → 시장 요약 → 대표 지수 카드 → 핵심 뉴스 카드 순서이며, 미국이 항상 먼저 온다.
- 아카이브 검색 결과는 클러스터 상세가 아니라 **날짜별 브리프**로 이동한다. 검색은 분석 화면이 아니라 과거 스냅샷으로 들어가는 탐색 허브다.
- 화면에 필요한 데이터는 페이지 단위 스냅샷 한 번의 응답으로 렌더링된다. 원문 기사 링크를 위한 추가 호출이 발생하지 않는다.
- 데이터 원천은 외부 뉴스 검색 API와 지수 provider이며, 요약·해설은 AI가 배치 시점에 생성한다. 실시간 데이터가 아니다.

## Capabilities and Constraints

- **기준일은 항상 한국 시간(UTC+9) 기준 날짜**다. `businessDate`(시장 결과가 귀속되는 날짜)와 `generatedAt`(스냅샷 생성 시각)은 분리해 표기한다.
- **부분 실패는 정식 상태다.** 페이지 상태는 `READY` / `PARTIAL` / `FAILED`, 배치 상태는 `SUCCESS` / `PARTIAL` / `FAILED`. 실패를 숨기지 않고, `partialMessage`로 무엇이 빠졌는지 밝히는 것이 제품 규칙이다. 일부 지수·기사·AI 요약이 빠져도 가능한 범위까지 렌더링한다.
- 재생성은 덮어쓰지 않고 항상 새 `versionNo`를 만든다. 과거 브리프는 저장된 그대로 다시 그릴 수 있어야 한다.
- 시장 범위는 미국·한국 두 곳으로 고정. 추가 국가/자산군, 실시간 차트·호가·개별 종목 티커, 개인화 피드는 범위 밖이다.
- 데이터 하한: 대표 지수는 미국 3개·한국 2개 이상 권장, 핵심 뉴스 클러스터 카드는 시장별 2개 이상.
- 조회 응답 목표는 평균 2초 이내(최신/날짜별/검색/클러스터 상세 공통).
- 운영 로그와 원본 에러 메시지는 일반 사용자 화면에 노출하지 않는다.
- 라이트/다크 테마 전환은 공통 UI 레벨에서 지원하며 사용자 선택이 저장된다.
- 화면 문구의 기본 언어는 한국어다(`<html lang="ko">`).
- **미결정으로 남아 있는 제품 사실** — 이후 작업이 임의로 정하지 말 것:
  - AI 요약 실패 시 fallback 문구 노출 기준과 `PARTIAL` 처리 경계
  - 운영 페이지를 내부 전용으로 고정할지 여부
  - 과거 버전(`versionNo`) 보관 기간 정책

### 용어

`businessDate`(시장 결과 귀속 기준일), `generatedAt`(스냅샷 생성 시각), `globalHeadline`(미국+한국을 묶는 한줄 요약), market section(브리프 내부의 미국 또는 한국 섹션), cluster(동일 이슈로 묶인 기사 집합), snapshot page(특정 날짜의 최종 렌더링용 통합 페이지 데이터), `partialMessage`(누락·실패 설명 메시지).

## Brand Commitments

- **제품 이름은 아직 확정되지 않았다.** 현재 리포지토리에 `Market Daily Brief`(PRD), `stockfront`(브라우저 타이틀·리포지토리명), `AI 시장 브리프` / `일간 시장 브리프 · 운영 콘솔`(앱 셸 태그라인, 권한별 분기)이 공존한다. 이후 작업은 이 셋 중 하나를 임의로 정식 명칭으로 승격시키지 말고, 이름이 필요할 때 사용자에게 확인한다.
- 확정된 로고, 브랜드 팔레트, 타이포그래피, 목소리 가이드는 아직 없다.

## Evidence on Hand

- 제품 문서: `docs/Product_Requirement_Document.md`, `docs/Information_Architecture.md`, `docs/UI_Requirement_Document.md`, `docs/Wireframe_Document.md`
- API 계약: `docs/api_spec_doc.md`, `docs/api-spec.json`, 통합 인수인계 `docs/fe-be-contract-handoff-2026-08-14.md`
- 구현된 화면 5종 전부(`src/pages/*`)와 공통 앱 셸이 실제로 동작한다. 인증은 `${VITE_API_HOST}/api/user/token`에 의존한다.
- 검증 자산: Vitest 단위/컴포넌트 테스트, Playwright e2e(`e2e/*` — 접근성, 반응형, 권한, 라우팅 등), 대비 감사 스크립트 `pnpm contrast`, 신뢰성 감사 `docs/reliability-audit.md`
- **없는 것(지어내지 말 것):** 실사용자 수치, 고객 사례·후기, 벤치마크, 가격/라이선스, 공개 배포 실적. 로고와 브랜드 자산도 없다. `public/favicon.svg`와 `public/icons.svg`가 유일한 그래픽 자산이다.

## Product Principles

1. **하루의 결과는 하나의 페이지다.** 미국과 한국을 따로 부르지 않고, 최신과 과거를 같은 템플릿으로 재현한다.
2. **기사가 아니라 이슈를 보여준다.** 소비 단위는 개별 기사 목록이 아니라 클러스터 카드이고, 원문 링크는 요약을 검증하는 수단으로 뒤에 붙는다.
3. **결손을 숨기지 않는다.** 부분 실패는 정상 상태의 하나로 취급하고, 무엇이 왜 빠졌는지 해당 위치에서 밝힌다.
4. **보는 사람에 따라 말을 바꾼다.** 같은 사실도 운영자에게는 진단 정보로, 일반 사용자에게는 다음 행동으로 번역해 전달한다.
5. **위에서부터 이해된다.** 전체 분위기 → 시장별 숫자 → 이슈 → 근거(원문·메타) 순서를 모든 브리프 화면이 공유한다.

## Accessibility & Inclusion

WCAG 2.1 AA를 준수한다. 명암비 4.5:1(대형 텍스트 3:1), 전체 기능의 키보드 조작 가능, 가시적인 포커스 표시가 구속력 있는 기준이다. 라이트/다크 두 테마 모두에서 충족해야 하며, `pnpm contrast`와 `e2e/a11y.spec.ts`(스킵 링크, Tab 순서, 드로어/다이얼로그 포커스 트랩과 Esc 복귀)가 현재의 검증 수단이다.
